# PERN Boiler with basic user model

# Installation Steps

- run `npm i` to install packages
- Change database configuration in config/config.json
- Create local and prod database if not done so already
- run `npx sequelize db:migrate`
